(namestring "getty")
(setq q (make-pathname :host "kathy"
                       :directory
                       (pathname-directory *default-pathname-defaults*)                      :name "getty"))
(setq q (make-pathname :host "kathy"
                       :directory
                       (pathname-directory *default-pathname-defaults*)
                       :name "getty"))

(setq q (make-pathname :host "kathy" 
                       :directory 
                       (pathname-directory *default-pathname-defaults*)
                       :name "getty"))

; People who specify the end are insufferable to me, as if the end, which lay in the future, could be predicated in advance, such is the insufferability of accounting retards.
